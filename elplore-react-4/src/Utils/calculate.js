const add = (first, second) =>{
    return first + second;
}
const multiply = (first, second) =>{
    return first * second;
}
const devideTheNumberByOtherNumber = (first, second) =>{
    return first / second;
}

// export default add;
export { add, multiply, devideTheNumberByOtherNumber }